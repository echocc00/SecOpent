import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function ApprovalCenter() {
  return (
    <PagePlaceholder
      title="Approval Center"
      milestone="W7"
      description="Pending plan approvals: review the plan + scope digests, approve or reject (human-only decision), and audit history."
    />
  );
}
