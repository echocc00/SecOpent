import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function Updates() {
  return (
    <PagePlaceholder
      title="Updates"
      milestone="W4c"
      description="Knowledge-layer health: the 5 detectors (source stale / curation lag / coverage regression / source unreachable / signature invalid), bundle history, and sync status."
    />
  );
}
