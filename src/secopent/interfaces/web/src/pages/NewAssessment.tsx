import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function NewAssessment() {
  return (
    <PagePlaceholder
      title="New Assessment"
      milestone="W5"
      description="Five-step wizard: select project, define & freeze scope, choose tools per coverage domain, generate the execution plan, and submit for approval."
    />
  );
}
