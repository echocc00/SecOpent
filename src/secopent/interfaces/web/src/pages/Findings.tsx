import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function Findings() {
  return (
    <PagePlaceholder
      title="Findings"
      milestone="W4b"
      description="Filterable findings table with severity badges, the three-layer evidence viewer (raw / redacted / summary), and the coverage matrix."
    />
  );
}
